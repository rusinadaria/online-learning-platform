import React, {FC} from 'react';
import RegForm from '../components/RegForm';

const About: FC = () => {
    return (
        <div>
            <RegForm/>
        </div>
    );
};

export default About;